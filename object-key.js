const computer ={
    brand:'lenovo',
    price:35000,
    processor:'intel',
    ssd:'512gb'
}
//for checking keys
// const keys = Object.keys(computer);
// console.log(keys);

//For checking values

const values= Object.values(computer);
console.log(values);
